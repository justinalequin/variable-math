const a = 1;
const b = 10;
const c = 2;

const square = Math.sqrt (b)**2 - 4 * (a * c);

const x1 = (-1 * (b) + (square)) / (2 * (a));

const x2 = (-1 * (b) - (square)) / (2 * (a));

console.log("x1 = ", (x1), "and x2 = ", (x2));


